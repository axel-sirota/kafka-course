import { Sink, Source } from 'most';
import { MulticastSource } from '@most/multicast';
export declare class HoldSubjectSource<T> extends MulticastSource<T> {
    protected has: boolean;
    protected buffer: Array<T>;
    protected bufferSize: number;
    constructor(source: Source<T>, bufferSize: number);
    add(sink: Sink<T>): number;
    event(time: number, value: T): void;
}
export declare function dropAndAppend<T>(value: T, buffer: T[], bufferSize: number): T[];
