import { Stream, Source, Sink } from 'most';
import { Subject } from '../../interfaces';
export declare function sync<T>(): SyncSubject<T>;
export declare function asSync<T>(stream: Stream<T>): SyncSubject<T>;
export declare class SyncSubject<T> extends Stream<T> implements Subject<T> {
    source: Source<T> & Sink<T>;
    constructor(source: Source<T> & Sink<T>);
    next(value: T): this;
    error<Err extends Error>(err: Err): this;
    complete(value?: T): this;
}
