/// <reference types="node" />
import { CompressionTypes, Kafka, Message, ProducerConfig } from "kafkajs";
import { Writable } from "stream";
interface ProducerObjectStreamOptions {
    config?: ProducerConfig;
    topic: string;
    acks?: number;
    timeout?: number;
    compression?: CompressionTypes;
    highWaterMark?: number;
    transform?: (data: any) => Message;
}
export declare class ProducerObjectStream extends Writable {
    constructor(kafka: Kafka, options: ProducerObjectStreamOptions);
    private producer;
    private options;
    private connected;
    _writev(chunks: {
        chunk: any;
        encoding: string;
    }[], callback: (error?: Error | null) => void): void;
    _destroy(error: Error | null): void;
}
export {};
