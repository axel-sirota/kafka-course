/// <reference types="node" />
import { ConsumerConfig, Kafka, KafkaMessage } from "kafkajs";
import { Readable } from "stream";
interface ConsumerObjectStreamOptions {
    config?: ConsumerConfig;
    topics: {
        topic: string | RegExp;
        fromBeginning?: boolean;
    }[];
    highWaterMark?: number;
    transform?: (data: KafkaMessage) => any;
}
export declare class ConsumerObjectStream extends Readable {
    constructor(kafka: Kafka, options: ConsumerObjectStreamOptions);
    private consumer;
    private kafka;
    private options;
    private connected;
    private started;
    private paused;
    private init;
    _read(): void;
    private start;
    private onCrash;
    private run;
    _destroy(error: Error | null): void;
}
export {};
